services:
  frontend:
    ports:
      - 3000:3000
    build:
      target: base
    volumes:
      - ../frontend:/app
    command: [npm, start]
  api:
    environment:
      PORT: 80
    ports:
      - 4000:80
    volumes:
      - ../api:/app
    command: ["npm", "run", "dev"]
    build:
      target: base
